import react from 'react';
import ReactDOM from 'react-dom';

const elem = (props) => {
    return (
        <div style={{textAlign: 'left', marginLeft: "2.5%"}}>
            <h2>Pineapple CV</h2>
        </div>
    )
};

export default elem;